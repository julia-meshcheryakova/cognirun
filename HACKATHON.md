# Hackathon docs moved

All RUN/HACK strategy, phases, prompts and plans now live in the dedicated project:

  ../runhack/   (start at README.md)

This folder (cognirun-app) holds only the APP CODE — the reference build.
